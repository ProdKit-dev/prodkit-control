name: V090 Write Probe

on:
  push:
    branches: [release/v0.9.0-cumulative-completeness]

permissions:
  contents: write

jobs:
  probe:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - run: echo "v0.9 write probe"
