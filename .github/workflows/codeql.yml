name: CodeQL

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  codeql:
    name: codeql
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "disposable cleanup operator branch"
