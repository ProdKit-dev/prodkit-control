# Disposable operator-branch override. Never merge.
name: CodeQL

on:
  pull_request:
    branches: [main]

jobs:
  disabled-on-cleanup-operator:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
