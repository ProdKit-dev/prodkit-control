name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: SemVer without v prefix
        required: true
        type: string
      prerelease:
        description: Mark GitHub Release as prerelease
        required: true
        default: false
        type: boolean

permissions:
  contents: write
  actions: read
  id-token: write
  attestations: write
  artifact-metadata: write

concurrency:
  group: release-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  proof-gate:
    name: Verify release-candidate proof
    runs-on: ${{ fromJSON(vars.PRODKIT_RUNNER_JSON != '' && vars.PRODKIT_RUNNER_JSON || '["self-hosted","Linux","X64"]') }}
    timeout-minutes: 10
    env:
      TARGET_SHA: ${{ github.sha }}
      PROOF_WORKFLOW_FILE: .github/workflows/trusted-release-proof.yml
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Verify exact current main and successful proof
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF" = "refs/heads/main"
          python3 - <<'PY'
          import json
          import os
          import urllib.parse
          import urllib.request

          sha = os.environ["TARGET_SHA"]
          if len(sha) != 40 or any(ch not in "0123456789abcdef" for ch in sha):
              raise SystemExit("target SHA must be an exact lowercase 40-character commit SHA")
          repo = os.environ["GITHUB_REPOSITORY"]
          workflow_file = os.environ["PROOF_WORKFLOW_FILE"]
          token = os.environ["GH_TOKEN"]
          params = urllib.parse.urlencode(
              {"head_sha": sha, "event": "workflow_dispatch", "status": "completed", "per_page": 100}
          )
          request = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/actions/runs?{params}",
              headers={
                  "Authorization": f"Bearer {token}",
                  "Accept": "application/vnd.github+json",
                  "X-GitHub-Api-Version": "2022-11-28",
                  "User-Agent": "prodkit-control-release-proof-gate",
              },
          )
          with urllib.request.urlopen(request, timeout=120) as response:
              runs = json.load(response).get("workflow_runs", [])
          matches = [
              run
              for run in runs
              if run.get("path") == workflow_file
              and run.get("head_sha") == sha
              and run.get("event") == "workflow_dispatch"
              and run.get("status") == "completed"
              and run.get("conclusion") == "success"
          ]
          if not matches:
              raise SystemExit(
                  f"missing successful workflow_dispatch proof from {workflow_file!r} for exact SHA {sha}"
              )
          print(f"release-candidate proof: {matches[0].get('html_url')}")
          PY

  release:
    name: release
    needs: proof-gate
    uses: ProdKit-dev/prodkit-workflows/.github/workflows/reusable-release.yml@0ab9021d06a59ffeb0a712f75fc963781fd8d739
    with:
      version: ${{ inputs.version }}
      target_sha: ${{ github.sha }}
      prerelease: ${{ inputs.prerelease }}
      runner_json: ${{ vars.PRODKIT_RUNNER_JSON != '' && vars.PRODKIT_RUNNER_JSON || '["self-hosted","Linux","X64"]' }}
      required_workflows_json: '["CI","Security","CodeQL"]'
      python_enabled: true
      python_version: "3.13"
      uv_version: "0.10.0"
      node_enabled: true
      node_version: "24"
      pnpm_version: "10.15.0"
      generate_sbom: true
      # GitHub-native attestation persistence is unavailable for this private
      # repository under the current organization plan. Exact-source proof,
      # SHA256SUMS, release metadata, and SPDX SBOM remain mandatory.
      attest: false
      environment: release
