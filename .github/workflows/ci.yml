# Disposable operator-branch override. Never merge.
name: CI

on:
  pull_request:
    branches: [main]

jobs:
  disabled-on-cleanup-operator:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
