name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    name: ci
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "disposable cleanup operator branch"
