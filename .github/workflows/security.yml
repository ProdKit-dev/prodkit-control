name: Security

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  security:
    name: security
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "disposable cleanup operator branch"
