name: Security

on:
  pull_request:
    branches: [main]

jobs:
  skip-finalizer:
    if: github.event.pull_request.head.ref != 'ops/v0.4.0-finalize'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security is intentionally skipped on the disposable v0.4.0 finalizer PR."
